This repo shows two methods to add observability to data fetching in  react applications.

1. Replace the windows fetch method with a custom function
2. Use a provider and a hook to behave like fetch.